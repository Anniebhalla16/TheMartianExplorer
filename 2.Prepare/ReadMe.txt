Tecnhnology: XSLT 3., applied using python's lxml module, to convert 
raw HTML transformed JSON into canonical XML

TO START EXISTDB LOCALLY
1. docker run -d --name existdb -p 8080:8080 existdb/existdb:latest
2. docker stop existdb
3. docker rm existdb